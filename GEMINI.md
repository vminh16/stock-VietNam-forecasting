@AGENTS.md

## Gemini CLI

Everything project-specific lives in the files `AGENTS.md` points to: rules
above, the contract in `SPEC.md`, the experiment map in `docs/experiments.md`.
This file holds nothing else, so there is one place to keep current.

The skills table in `AGENTS.md` names skills stored in `.agents/skills/`.
